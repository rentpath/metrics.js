{
    "name": "metrics",
    "version": "1.2.0",
    "description": "A cli tool to retrieve performance metrics from a control url and compare it to a target url.",
    "main": "metrics.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "buildalpine": "pkg metrics --target \"host\" --output \"metrics-alpine\""
    },
    "author": "",
    "license": "MIT",
    "dependencies": {
        "commander": "^2.17.1",
        "markdown-table": "^1.1.2",
        "psi": "^3.1.0",
        "puppeteer": "^1.7.0"
    }
}
